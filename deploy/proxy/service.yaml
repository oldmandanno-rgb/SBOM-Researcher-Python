apiVersion: v1
kind: Service
metadata:
  name: osv-proxy
  labels:
    app.kubernetes.io/name: osv-proxy
    app.kubernetes.io/part-of: sbom-researcher
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: osv-proxy
  ports:
    - name: https
      port: 443
      targetPort: https
      protocol: TCP
